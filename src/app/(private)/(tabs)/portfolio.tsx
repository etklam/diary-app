import { Workspace } from '@/navigation/workspace';
export default function Portfolio() {
  return <Workspace section="portfolio" title="Portfolio" description="Portfolio tools are not available yet. No balances or results are shown here." />;
}
